{% extends 'backend/backbody.html.twig' %}
{% block content %}

    {{ form_start(form,{attr:{'id':'form'}}) }}
    <div class="container">
        <div class="form-group">
            {{ form_label(form.titre,"Titre du forum:") }}

            {{ form_widget(form.titre, {attr: {class:'form-control'}}) }}
        </div>
        <div class="form-group">
            {{ form_label(form.description,"description:" ) }}

            {{ form_widget(form.description,{attr: {class:'form-control'}}) }}
        </div>
        {# <div class="form-group">
            {{ form_label(form.imageFile,"image:") }}

            {{ form_widget(form.imageFile,{ 'attr': {'id': 'image'} }) }}
        </div> #}

    </div>
    <div class="card-footer">
        <button type="submit" href="{{ path('add_forum') }}" class="btn btn-primary">Submit</button>
    </div>




    {{ form_end(form) }}
{% endblock content %}