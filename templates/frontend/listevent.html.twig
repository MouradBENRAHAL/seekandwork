{% extends 'frontend/frontheaderfooter.html.twig' %}
{% block content %}
    <div class="card">
    <div class="card-header">
        <h3 class="card-title">LES evenements</h3>
    </div>

    <div class="card-body p-0">
        <table id="chauffeur" class="table table-striped">
            <thead>
            <tr >



            </tr  >
            </thead>
            <tr {% for c in event %} >


                <td> {{  c.titre }}</td>
                <td> {{  c.description }}</td>
                <td> <img width="200" src="{{ asset('image/'~ c.image)}}"></td>
                <td> {{  c.type }}</td>
                <td><a class="btn btn-block btn-success" href="{{ path('list_specialisationfront',{'idevent':c.id}) }}" > voir plus </a></td>



            </tr {% endfor %} >


        </table>

    </div>
{% endblock content %}